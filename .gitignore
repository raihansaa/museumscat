__pycache__/
*.pyc
.pytest_cache/
.build/
outputs/

# API keys are read from the environment; never commit a local env file.
.env
.env.*

# Per-row competition data is deliberately not redistributed (gold strings).
data/
